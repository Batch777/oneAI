# 本轮实施记录

## 已完成

- 旧 TUI、Python Runtime、终端图片/滚轮模块移到 `oneai/legacy/`；测试、示例和截图脚本分别隔离。原 `ask` 改为 `ask-legacy`，旧导入需迁移。Textual/Pillow 改成可选依赖；Outlook MSAL 也独立为可选依赖。
- 默认 Python 测试只收集主路径；增加统一 `scripts/check.py`，执行 Python 和 Node 桥接测试。
- 检索和单轮草稿先同步文件 hash；增删改名更新索引；独立 watch 支持持续对账。
- 索引重建事务化，失败回滚。旧索引格式自动升级并在检索前重建。
- 原文版本保存到独立 `sources.sqlite`；当前文件改动、删除、甚至索引被删除后，仍可按版本回读已有证据。标题检索元数据与原文引用文本分离。
- vault 读取/写入和单文件索引拒绝绝对路径、上级目录和外部符号链接；行范围验证；草稿原子保存。
- pi 起草使用当前会话正文，走 stdin 原样传到保存接口，不再调用第二个模型；来源版本和行范围在保存前检查。
- pi 每轮从用户 identity 和 rules 重新加载上下文；独立 CLI 起草也加载相同规则。保存时记录可见规则版本，不等同于模型行为保证。
- 当前本地草稿没有对外副作用，不新增批准弹窗；发送/提交尚未实现。归档 Runtime 的无回调审批改为拒绝。
- 启动器显式加载扩展并传递 Python 环境，去掉个人绝对路径依赖。
- 重写当前架构文档；另建未来产品 spec、论文研究报告。修正 iOS 和实验 Outlook/ledger 注释中的已实现误述。

## 验证

- Python 主路径：55 passed（追加 10 项 Outlook 回归和 1 项 PDF 去重、编码修复、页码与删除回归）。
- Node 桥接与实际 pi 扩展：5 passed，真实加载本机 pi 0.86.1 并在临时库执行 search/read/draft_create；未调用真实模型或邮箱。
- 归档 runtime/images 迁移抽查：20 passed。未重跑全部旧 UI/剪贴板测试。
- Python 编译检查与 git diff --check 通过。
- 界面草图经过独立浏览器检查：无页面错误；360px 手机宽度无水平溢出；编辑后批准失效、证据展开、标签切换和规则暂停交互通过。草图按钮不连接真实服务。

## 明确未完成

尚无已部署的云端任务服务、持久审批、发送闭环或手机 API。Outlook delta 现在已具备离线回归测试和部署单元，但真实授权与云端运行仍待完成。PDF 本地试点索引了 30 篇、795 页；独立实验脚本尚未接入主检索。实验 ledger 未升级为可靠任务执行器。

论文研究是官方文档调研与可执行评测方案，不是私有语料实测；未安装大型模型或读取个人论文。下一阶段先做 50 篇代表样本试点。

历史评审 REVIEW-2026-09-21.md 保留原审计事实；其源码路径和“未修复”描述对应本轮改动之前，当前状态以本记录和 ARCHITECTURE.md 为准。
