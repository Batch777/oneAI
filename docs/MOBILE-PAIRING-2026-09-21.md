# 手机换行与四位配对码

检索结果此前使用 white-space: pre-wrap，保留 PDF 双栏提取文本里的物理断行。卡片现改为 normal，按容器宽度排版，并允许长串换行；搜索输入框允许 flex 收缩。资料原文和版本引用未更改，原文弹窗仍保留提取文本行序。

用户确认将公网配对码改为四位数字。码有效十分钟，仅使用一次；仅保留一个有效码，生成新码会使上一个未用码失效。生成使用数据库写事务，避免并发留存多个有效短码。单 IP 十分钟最多失败五次，全站十分钟最多失败十次；重新生成配对码不会清空失败记录。已登录设备不受登录尝试锁定影响。此短码仍存在小概率猜中风险；限额耗尽后需等待窗口过去。

本地 95 项测试通过。云端候选版本 7 项 Web 测试通过，再备份并替换部署文件、重启 Web 服务。Web、后台任务和 Outlook 收信服务均 active。公网页面实际生成四位码。代码检查覆盖旧码替换、过期、重复使用及 IP/全站失败限额。
